<h1><%= @task.title %></h1>
<p><%= @task.description %></p>
<p><%= @task.due_date %></p>
<p><%= @task.completed %></p>

<%= link_to 'Edit', edit_task_path(@task), class: 'btn btn-primary' %>
<%= link_to 'Back', tasks_path, class: 'btn btn-primary' %>
<%= link_to "Delete", task_path(@task), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to remove this note? This will remove any task it has."}, class: 'btn btn-danger' %>
<% @tasks.each do |task| %>
  <div class="card-product">
    <% task.photos.each do |photo| %>
      <div class="card-product-image">
        <%= cl_image_tag(photo.key, height: 300, width: 400, crop: :fill) %>
      </div>
    <% end %>
    <div class="card-product-infos">
      <h2><%= task.title %></h2>
      <p><%= task.description %></p>
    </div>
  </div>
<% end %>
